apiVersion: v1
kind: PersistentVolume
metadata:
  name: service-festplatte
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  hostPath:
    path: /mnt/data
